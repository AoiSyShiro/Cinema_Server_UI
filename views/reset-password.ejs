<!DOCTYPE html>
<html lang="vi">
<!-- Định nghĩa tài liệu HTML và ngôn ngữ là tiếng Việt -->

<head>
    <meta charset="UTF-8">
    <!-- Định nghĩa bộ mã hóa ký tự là UTF-8 để hỗ trợ các ký tự đặc biệt, bao gồm tiếng Việt -->

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Đảm bảo trang web phản hồi tốt trên các thiết bị di động bằng cách thiết lập chiều rộng của viewport bằng chiều rộng của thiết bị -->

    <title>Đặt lại mật khẩu</title>
    <!-- Tiêu đề của trang web sẽ hiển thị trên tab của trình duyệt -->

    <!-- Thêm link đến thư viện CSS Tailwind -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Thư viện CSS để tạo kiểu dễ dàng với các lớp tiện ích có sẵn của Tailwind -->
    
    <!-- Thêm thư viện jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- Thư viện jQuery giúp thao tác với DOM và thực hiện các yêu cầu AJAX đơn giản -->
</head>

<body class="bg-gray-100 flex items-center justify-center h-screen">
    <!-- Thân trang có nền màu xám nhạt (bg-gray-100), dùng flexbox để căn giữa nội dung (items-center justify-center),
         và chiều cao của màn hình sẽ chiếm toàn bộ (h-screen) -->

    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-sm">
        <!-- Khối chứa form với nền trắng (bg-white), padding xung quanh (p-8), bo tròn các góc (rounded-lg),
             đổ bóng (shadow-lg), chiều rộng đầy đủ (w-full) và giới hạn chiều rộng tối đa (max-w-sm) -->

        <h2 class="text-2xl font-bold text-center text-gray-800 mb-6">Đặt lại mật khẩu</h2>
        <!-- Tiêu đề "Đặt lại mật khẩu" với cỡ chữ 2xl, đậm (font-bold), canh giữa (text-center), màu xám đậm (text-gray-800),
             và khoảng cách dưới (mb-6) -->

        <!-- Form reset password -->
        <form id="reset-password-form">
            <!-- Form này sẽ gửi thông tin đến server để đặt lại mật khẩu -->

            <!-- Hidden Token -->
            <input type="hidden" name="token" value="<%= token %>">
            <!-- Trường ẩn chứa token dùng để xác thực yêu cầu reset mật khẩu. Token này sẽ được cung cấp từ server (ví dụ qua EJS) -->

            <!-- New Password -->
            <div class="mb-4">
                <!-- Khối này chứa trường nhập mật khẩu mới -->

                <label for="new-password" class="block text-gray-700 font-medium mb-2">Mật khẩu mới</label>
                <!-- Nhãn cho trường nhập mật khẩu mới, với màu xám nhạt (text-gray-700) và khoảng cách dưới (mb-2) -->

                <input type="password" id="new-password" name="newPassword" placeholder="Nhập mật khẩu mới"
                    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    required>
                <!-- Trường nhập mật khẩu mới (password field) với các kiểu dáng từ Tailwind CSS. Đảm bảo trường này là bắt buộc (required) -->
            </div>

            <!-- Confirm Password -->
            <div class="mb-6">
                <!-- Khối này chứa trường xác nhận mật khẩu -->

                <label for="confirm-password" class="block text-gray-700 font-medium mb-2">Xác nhận mật khẩu</label>
                <!-- Nhãn cho trường xác nhận mật khẩu -->

                <input type="password" id="confirm-password" name="confirmPassword" placeholder="Xác nhận mật khẩu"
                    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    required>
                <!-- Trường xác nhận mật khẩu, yêu cầu người dùng nhập lại mật khẩu mới -->
            </div>

            <!-- Error Message -->
            <div id="error-message" class="text-red-500 text-sm mb-4" style="display:none;"></div>
            <!-- Vùng hiển thị thông báo lỗi nếu có. Ban đầu nó ẩn đi -->

            <!-- Success Message -->
            <div id="success-message" class="text-green-500 text-sm mb-4" style="display:none;"></div>
            <!-- Vùng hiển thị thông báo thành công nếu có. Ban đầu nó cũng ẩn đi -->

            <!-- Submit Button -->
            <div class="flex justify-center">
                <button type="submit"
                    class="w-full bg-blue-600 text-white font-semibold py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50">
                    Đặt lại mật khẩu
                </button>
                <!-- Nút gửi form để đặt lại mật khẩu, khi hover sẽ đổi màu nền -->
            </div>
        </form>
    </div>

    <script>
        // AJAX request on form submission
        $('#reset-password-form').on('submit', function(e) {
            e.preventDefault(); // Ngừng hành động mặc định của form (submit)

            const token = $('input[name="token"]').val(); // Lấy giá trị của token từ trường ẩn
            const newPassword = $('#new-password').val(); // Lấy mật khẩu mới từ trường nhập
            const confirmPassword = $('#confirm-password').val(); // Lấy xác nhận mật khẩu

            // Clear previous messages
            $('#error-message').hide().text(''); // Ẩn thông báo lỗi và xóa nội dung cũ
            $('#success-message').hide().text(''); // Ẩn thông báo thành công và xóa nội dung cũ

            // Thực hiện yêu cầu AJAX để gửi dữ liệu tới server
            $.ajax({
                type: 'POST', // Phương thức POST
                url: '/reset-password/reset-password', // Địa chỉ URL để gửi yêu cầu
                data: {
                    token: token, // Gửi token
                    newPassword: newPassword, // Gửi mật khẩu mới
                    confirmPassword: confirmPassword // Gửi xác nhận mật khẩu
                },
                success: function(response) {
                    if (response.success) {
                        $('#success-message').text(response.message).show(); // Hiển thị thông báo thành công nếu response.success là true
                    } else {
                        $('#error-message').text(response.message).show(); // Hiển thị thông báo lỗi nếu response.success là false
                    }
                },
                error: function() {
                    $('#error-message').text('Có lỗi xảy ra. Vui lòng thử lại sau!').show(); // Hiển thị thông báo lỗi nếu yêu cầu AJAX gặp sự cố
                }
            });
        });
    </script>

</body>
</html>
